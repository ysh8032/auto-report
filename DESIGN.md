# 디자인 노트

대시보드·리포트 UI를 다듬으면서 사용한 기술과, 대화 중에 정해진 디자인 원칙을 정리한 문서입니다.
새로운 화면을 추가하거나 스타일을 건드릴 때 이 문서를 먼저 참고하면 같은 시행착오를 반복하지 않습니다.

> **이 문서의 출처:** `customer-churn-dashboard`(2~4주차에 만든 대시보드)에서 이식했습니다. 아래 사례 중 "대시보드 ①~⑤ 차트", "원인 분석 카드 4개", "개선 제안 우선순위 표" 같은 구체적 예시는 그 앱에서 실제로 겪은 일이며 이 앱(`auto-report`)에는 같은 화면이 없습니다. **원칙은 그대로 적용하고, 예시는 그 원칙이 왜 생겼는지 보여주는 근거로 읽습니다.**
>
> 이 앱에만 적용되는 추가 규칙(상태 색 대응 등)은 [CLAUDE.md](./CLAUDE.md) 7절에 있습니다.

## 1. 사용한 기술·도구

| 목적 | 기술 | 비고 |
|---|---|---|
| 멀티 페이지 구성 | `st.navigation` + `st.Page` | `pages/` 디렉터리 없이 함수 기반으로 구성 (`dashboard_page`, `report_page`) |
| 특정 영역만 CSS 적용 | `st.container(key="...")` → 자동 생성되는 `st-key-<key>` 클래스 | Streamlit이 공식 지원하는, 버전에 안전한 방식. `[class*="st-key-..."]` 부분 일치 선택자로 타겟팅 |
| 표 스타일링 | 커스텀 HTML `<table>` (직접 렌더링) | `st.dataframe`은 canvas 기반(glide-data-grid)이라 헤더 굵게/셀 중앙정렬 같은 CSS를 못 먹인다. 그래서 개선 제안 우선순위 표는 CSS로 커스텀 가능한 HTML 표로 직접 작성 |
| 굵은 글씨 렌더링 | `**bold**` → `<strong>bold</strong>` 정규식 변환 후 `unsafe_allow_html=True` | 한 문단에 `**bold**`가 여러 개 있으면 일부만 굵게 처리되고 나머지는 `**`가 그대로 노출되는 Streamlit 마크다운 렌더러 버그가 있어, 네이티브 파서를 아예 우회 |
| 차트 | Plotly (`plotly.express`, `graph_objects`) | 색상은 Tailwind 500 팔레트로 통일 (`common.py`의 `COLOR_*` 상수) |
| PDF 생성 | `fpdf2` (import명은 `fpdf`) | 시스템 의존성 없는 순수 파이썬 라이브러리라 Streamlit Cloud에 안전. `markdown=True`로 굵게 지원, `pdf.table()` 컨텍스트 매니저로 표 렌더링 |
| PDF용 한글 폰트 | Noto Sans KR (OFL 라이선스) 서브셋 | Malgun Gothic은 재배포 불가라 사용 불가. `fontTools`로 가변 폰트(wght 축)를 정적 Regular/Bold로 인스턴싱한 뒤, 실제 쓰이는 유니코드 범위만 서브셋팅해서 10.4MB → 2.5MB로 축소 (`fontTools`는 폰트 준비 단계에서만 쓰고 런타임 의존성 아님) |
| 캐싱 | `st.cache_data` | 데이터 로딩, BigQuery 조회, PDF 생성(`get_report_pdf_bytes`)에 적용 |
| 파일 다운로드 | `st.download_button` | PDF 다운로드 버튼 |
| 배포 전 검증 | 클린 가상환경 + `pip install --only-binary=:all: -r requirements.txt` | 로컬에 깔린 다른 패키지 영향 없이, Streamlit Cloud와 동일한 조건(핀 고정된 패키지만)으로 PDF 생성 기능을 재현 테스트 |
| CSS 검증 | Playwright (`getComputedStyle`, `scrollWidth` 등) | 폰트 크기 1pt 차이, 가로 스크롤 발생 여부 같은 걸 감으로 판단하지 않고 브라우저에서 직접 측정 |

### 알아두면 좋은 함정
- **Plotly `go.Indicator`(게이지)**: 공통 레이아웃 딕셔너리(`CHART_LAYOUT`)에 `title_font`가 들어있으면, 인디케이터 차트엔 figure-level 제목이 없는데도 빈 `layout.title` 객체가 생겨 Plotly.js가 "undefined" 텍스트를 그대로 그려버린다. 게이지류는 `title_font` 키를 빼고 레이아웃을 적용해야 함.
- **`fpdf2`는 상태 저장형(stateful)**: `set_draw_color()`로 한 번 색을 바꾸면 이후 그려지는 모든 선(표 테두리 포함)이 그 색을 물려받는다. 소제목 밑줄에 강조색(파란색)을 쓴 뒤 반드시 `INK`(검은색)로 되돌려야 표 테두리가 의도한 색으로 나온다.
- **`kaleido`(Plotly 정적 이미지 export)는 이 환경에서 불안정**: 크롬 프로세스가 여러 개 뜨면서 멈추는 현상이 확인됨(배포 환경에서는 더 위험). 그래서 PDF에는 차트 이미지를 넣지 않고 텍스트·표 중심으로 구성.

## 2. 대화에서 정해진 디자인 원칙

1. **트렌디한 색감 = 팔레트를 하나로 통일**
   임의로 고른 색 대신 Tailwind 500 계열 팔레트(blue `#3b82f6`, rose `#f43f5e`, emerald `#10b981`, slate `#64748b` 등)로 전체 차트·카드·배지 색을 통일했다.

2. **제목에 쓴 폰트는 본문에도 적용한다**
   타이틀에만 예쁜 폰트(Pretendard)를 쓰고 본문은 기본 폰트로 남겨두면 이질감이 생긴다. 단, Streamlit이 아이콘 렌더링에 쓰는 Material Symbols 폰트까지 덮어쓰면 사이드바 화살표 아이콘이 깨지므로, `[data-testid="stIconMaterial"]`는 예외로 둬야 한다.

3. **레이아웃은 어색한 분할보다 단순한 전체 폭을 우선한다**
   리포트 본문을 좌우 2단으로 나누니 그래프가 오른쪽에 낑겨 들어간 느낌이 나고 아래쪽에 여백만 남는 문제가 있었다. → 텍스트 전체 폭, 그 다음 차트 전체 폭 순서의 단순한 세로 흐름으로 변경. "예쁘게 나누기"보다 "안 어색하게"가 우선.

4. **화면에 이상한 값(undefined 등)이 보이면 그 자리에서 바로 고친다**
   원인을 대충 넘기지 않고, 라이브러리 레벨의 근본 원인(레이아웃 병합 시 빈 title 객체 생성)을 찾아 고쳤다.

5. **같은 종류의 카드/박스는 완전히 균등하게**
   원인 분석 카드 4개는 2x2 그리드로, 가로세로 크기를 전부 동일하게 맞추고 중앙 정렬한다. "비슷하게"가 아니라 "동일하게".

6. **표는 목적에 안 맞는 칸을 억지로 채우지 말고 없앤다**
   "비고" 칼럼처럼 표 안에 넣기엔 텍스트가 길고 애매한 정보는 표에서 빼고, 표 아래 별도 문단으로 풀어서 설명한다. 표는 표답게(숫자·짧은 값 중심으로) 유지.

7. **"문서 느낌"은 크기가 아니라 질감의 문제다**
   "A4 용지 같은 리포트"라는 요청을 문자 그대로 A4 픽셀 폭으로 좁혔더니 오히려 가로 스크롤이 생기는 역효과가 났다. → 실제로 원했던 건 그림자, 둥근 모서리, 배경과 구분되는 종이색 같은 **시각적 질감**이었지 물리적 크기 제약이 아니었다. 요청을 문자 그대로 해석하기 전에 "왜 그렇게 보이고 싶은가"를 먼저 확인해야 한다.
   - 이 원칙에 따라 컨테이너는 `max-width: 1200px`로 넓히고, 배경은 흰색 대신 연한 아이보리(`#fbf8f2`)로 종이 질감을 냈다.

8. **화면과 인쇄물의 시각 언어는 일관되어야 한다**
   PDF 표 테두리가 화면에는 없는 파란색으로 나온 건, 소제목 밑줄에 쓴 강조색이 이후 그려지는 선에 그대로 남아있었기 때문(라이브러리가 상태를 유지하는 걸 놓친 버그). 화면 쪽 스타일(검은 테두리)과 PDF가 다르게 보이면 안 된다는 원칙 아래 즉시 수정.

9. **박스 안 텍스트는 본문보다 정확히 1포인트 작게, 감이 아니라 측정으로 확인**
   카드 안 글자를 처음엔 본문보다 오히려 큰 크기(11pt, 본문을 12pt로 착각)로 잘못 설정했었다. Playwright로 실제 렌더링된 폰트 크기를 `getComputedStyle`로 측정(본문 14px)한 뒤에야 정확히 1pt 작은 9.5pt로 맞출 수 있었다. **눈대중이 아니라 실측**으로 검증한다.

10. **차트 제목은 실제 데이터 성격을 정확히 반영하되, 억지로 통일하지 않는다**
    대시보드 ①~⑤ 차트는 전부 이탈"률"(비율)을 보여주므로 제목 끝을 "이탈률"로 통일했지만, ⑥번은 개별 고객의 산점도(scatter)라 비율이 아니다. 처음엔 일관성을 위해 ⑥도 "이탈률"로 맞췄지만, 사용자가 "안 맞는 건 고치지 마"라고 한 뒤 "이탈분포"로 다시 바꿨다. → **형식적 통일성보다 내용적 정확성이 우선**이며, 어느 쪽을 택할지는 결국 작성자(사용자)의 판단.

11. **차트 이미지 안정성이 불확실하면 기능 범위를 줄이는 게 낫다**
    kaleido가 불안정하다는 걸 확인한 뒤, "일단 되게는 만들어놓고 나중에 고치자"가 아니라 PDF에서 차트 이미지 자체를 빼는 방향으로 스코프를 줄였다. 불안정한 걸 억지로 붙이지 않는다.

12. **로컬 개발 환경도 배포 환경과 동일한 데이터 경로를 taste한다**
    "로컬에서도 빅쿼리 라이브로 연결하지 마"라는 지시에 따라 `FORCE_SNAPSHOT = True`로 로컬·배포 모두 항상 스냅샷을 쓰도록 강제했다. 로컬에서만 라이브로 붙으면 배포 환경과 다른 걸 보고 있다는 걸 모른 채 작업하게 된다.

13. **"배포해도 되는지 확인해줘"는 로컬 성공과 다른 질문이다**
    PDF 기능이 로컬에서 되는 것과, Streamlit Cloud처럼 깨끗한 환경·핀 고정된 패키지만 있는 곳에서 되는 것은 다른 문제다. 클린 가상환경을 새로 만들어 `requirements.txt`만으로 설치하고 실제로 PDF를 생성해보는 식으로 **배포 환경을 흉내 낸 재현 테스트**를 거친 뒤에야 "된다"고 답했다.

14. **git 커밋/푸시는 명시적으로 요청받았을 때만**
    스타일이나 코드를 고친 직후에도 먼저 커밋·푸시하지 않고, "깃 푸시 하자" / "서버 올려봐" 같은 명확한 지시가 있을 때만 반영한다.

## 3. 배포 관련
배포 절차·트러블슈팅은 [DEPLOY.md](./DEPLOY.md)에 별도로 정리되어 있습니다.
