# auto-report — 월간 리포트 자동화 도구

이 문서는 이 앱을 만들고 고칠 때 지켜야 할 **대전제**다. 코드를 쓰기 전에 먼저 읽는다. 여기 적힌 결정을 바꾸려면 문서를 먼저 고치고, 그 이유를 갱신 이력에 남긴다.

---

## 1. 이 앱은 무엇인가

> **데이터 파일 하나를 넣으면, 지표 계산부터 이메일 초안까지 자동으로 흐르고, 사람은 네 번만 승인한다.**

매월 새 데이터가 도착할 때 같은 형식의 리포트를 반복해서 만들어야 하는 상황을 자동화한다. 사람이 하는 일은 파일을 넣고, 시스템이 낸 결과를 확인하고, 발송을 확정하는 것뿐이다. 숫자를 옮겨 적거나 문장을 쓰는 작업은 없다.

### 하지 않는 것

| 항목 | 이유 |
|---|---|
| 임의 형식의 파일 분석 | 이 앱은 **알려진 스키마의 새 기간 데이터**만 받는다. 매번 지표를 새로 설계하는 건 자동화가 아니다 |
| 지표 정의 편집 | 정의는 위키에서만 고친다. 앱은 읽기만 한다 |
| 실제 이메일 발송 | 6주차 범위는 초안 생성과 발송 확정까지다. SMTP 연결은 8주차 |
| 원인 분석·개선 제안 자동 작성 | 판단이 필요한 문장은 사람이 쓴다 |
| 원본 테이블 수정 | 업로드 데이터는 스테이징 테이블로만 올린다 |

---

## 2. 8단계 흐름

이 순서를 바꾸지 않는다. 각 단계는 앞 단계가 끝나야 실행된다.

| # | 단계 | 주체 | 산출물 |
|---|---|---|---|
| 1 | 데이터 파일 투입 | **사용자** | 업로드된 원본 파일 |
| 2 | 스키마 점검 → 지표 계산 | 시스템 | 계산 결과 표 (계산 가능 / 불가 + 이유) |
| — | **지표 확인 후 확정** | **사용자** | 승인 기록 |
| 3 | 검증 실행 | 시스템 | 검증 결과 (통과 / 경고 / 차단) |
| 4 | 대시보드 렌더링 | 시스템 | 화면 |
| 5 | **내용·검증 결과 확인** | **사용자** | 확인 기록 |
| 6 | 리포트 생성 | 시스템 | 리포트 문서 (대시보드와 별개, PDF 포함) |
| 7 | 이메일 초안 생성 | 시스템 | 제목·수신자·본문·첨부 목록 |
| 8 | **발송 확정** | **사용자** | 확정된 최종 파일 |

### 사용자 개입 원칙

**사용자는 넣기 한 번, 승인 세 번만 한다.** 승인 지점에서 사용자가 하는 일은 판정(진행/중단)이며, 내용을 직접 입력하거나 수정하는 일이 아니다.

- 승인 없이 다음 단계로 넘어가지 않는다. 단계 건너뛰기를 허용하지 않는다.
- 승인은 되돌릴 수 있다. 2단계로 돌아가면 3단계 이후 결과는 폐기하고 다시 계산한다.
- 각 승인 시점과 승인 대상(어느 파일, 어느 지표 버전)을 기록한다.

---

## 3. 위키 연동 구조

위키(`my-wiki-02`)는 Obsidian 개인 저장소이고 이 앱은 배포된다. **앱이 위키 폴더를 직접 읽으면 배포 환경에서 깨진다.**

```
[설정층 = 위키]  사람이 정의를 큐레이션한다
  my-wiki-02/06_metrics/*.md     지표 명세 17종
  my-wiki-02/02_data/*.md        테이블 스키마 (컬럼명·타입·의미)
        │
        │  export (파이프라인 0단계, 수동 실행)
        ▼
  catalog/metrics_catalog.json   프론트매터 + "답할 수 없는 것" 절
  catalog/schema_catalog.json    컬럼 목록·타입·연결 키
        │
        ▼
[실행층 = 앱]  읽기 전용으로 소비한다
```

**카탈로그가 두 개인 이유:** 지표 명세(`06_metrics`)만으로는 업로드 파일의 스키마를 판정할 수 없다. 정의서에는 계산에 쓰는 컬럼만 등장하므로, 테이블의 전체 컬럼 목록은 `02_data`의 스키마 노트에서 따로 가져와야 한다.

### 규칙

- **정의를 앱 코드에 쓰지 않는다.** 지표를 추가·수정하려면 위키 노트를 고치고 카탈로그를 다시 export한다. 앱 코드는 건드리지 않는다.
- `metrics_catalog.json`에는 **프론트매터(계산 명세)** 와 본문 중 **"이 지표로 답할 수 없는 것"** 절만 담는다. 판단 근거·대안 검토는 사람이 위키에서 읽을 것이므로 제외한다.
- `schema_catalog.json`에는 **컬럼명·타입·연결 키**를 담는다. 컬럼 설명은 화면에 표시할 용도로만 쓴다.

### 노트명과 테이블명이 다르다

`02_data` 노트명은 raw CSV 파일명을 따라 `data_` 접두사가 붙어 있으나, BigQuery 테이블명과 지표 정의서의 `계산.원천`에는 접두사가 없다. 그대로 대조하면 **계산 대상 지표가 0개**가 된다.

```
"usage_history": {
    "노트명": "data_usage_history",
    "테이블명": "usage_history",
    "테이블명_추정": true,
    ...
}
```

- `schema_catalog`의 **키는 테이블명**으로 한다. 지표 원천과 맞춰야 하기 때문이다.
- 테이블명은 프론트매터의 `bq_table` 필드를 우선 쓰고(`추정: false`), 없으면 노트명에서 `data_` 접두사와 확장자를 제거해 유도한다(`추정: true`).
- **추정 여부를 화면에 표시한다.** 조용히 추측하지 않는다. 정확하게 하려면 위키 노트에 `bq_table`을 추가한다.
- **매핑을 앱 코드에 하드코딩하지 않는다.** `if note == "data_usage_history"` 같은 코드는 금지다.

### 컬럼 표는 기계가 읽는 구역이다

스키마 노트의 컬럼 표는 사람이 보기엔 표지만, 앱에게는 업로드 파일과 대조할 컬럼 목록이다. 취소선·화살표·"실물명" 같은 편집 표시가 섞이면 컬럼명이 오염된다.

- 컬럼명 셀은 백틱과 공백만 정제한다.
- 취소선으로 감싸인 행은 **건너뛰고 경고**한다. 임의로 살리지 않는다.
- 화살표나 "실물명" 문구가 섞인 셀은 **파싱하지 않고 경고**한다.
- 경고가 나면 **파서를 고치지 말고 위키 노트를 정리한다.** 편집 표시를 해석하는 파서를 만들면 그 규칙이 앱에 쌓인다.
- 카탈로그가 없는 테이블(스키마 노트 미작성)은 **판정 불가**로 처리한다. 추측해서 통과시키지 않는다.
- 리포트와 화면에 **카탈로그 갱신일과 지표 개수**를 찍는다. 어느 정의로 계산한 결과인지 나중에 추적할 수 있어야 한다.
- 카탈로그와 위키가 어긋나면 앱은 카탈로그를 따른다. 대신 화면에 갱신일을 노출해 오래된 것을 알아챌 수 있게 한다.

---

## 4. 분석 워크플로우 10단계가 어디에 있는가

5주차까지 배운 10단계 중 **판단에 해당하는 두 단계만 앱 밖에 있다.**

| 워크플로우 | 위치 | 주체 |
|---|---|---|
| 1. Define | 위키 `01_questions/` | **사람** (앱 밖) |
| 2. Acquire | 앱 1단계 | 사람 (파일 투입) |
| 3. Profile | 앱 2단계 | 시스템 |
| 4. Confirm | 앱 2단계 | 시스템 |
| 5. Clean | 앱 2단계 | 시스템 |
| 6. Model | 위키 `06_metrics/` | **사람** (앱 밖) |
| 7. Analyze | 앱 2단계 | 시스템 |
| 8. Validate | 앱 3단계 | 시스템 |
| 9. Publish | 앱 4·6단계 | 시스템 |
| 10. Approve&Send | 앱 7·8단계 | **사람** (승인) |

Define과 Model이 앱 밖에 있는 것은 설계 실수가 아니라 의도다. 이 둘이 판단이고 나머지는 실행이다.

---

## 5. 확정된 설계 결정

바꾸려면 이 문서를 먼저 고친다.

### 5-1. 투입 파일 = 알려진 스키마의 새 기간 데이터

- 예: `usage_history` 2025-01분처럼 컬럼 구조는 기존과 같고 기간이 새로운 파일.
- 임의 형식 파일은 받지 않는다. 스키마가 카탈로그의 어느 원천 테이블과도 맞지 않으면 **2단계에서 중단하고 이유를 표시한다.**
- 스키마 판정은 컬럼명·타입·필수 컬럼 존재 여부로 한다. 컬럼 순서는 보지 않는다.

### 5-2. 계산 엔진 = BigQuery

- 업로드 파일은 **매번 교체되는 스테이징 테이블**로 올린다. `CREATE OR REPLACE TABLE`(DDL)만 쓴다.
- **`INSERT`·`UPDATE`·`DELETE`·`MERGE`를 쓰지 않는다.** DML은 BigQuery 샌드박스에서 차단되므로, 결제 계정이 없는 사용자도 이 앱을 돌릴 수 있어야 한다.
- 원본 테이블에 데이터를 붙이지 않는다. 스테이징 테이블과 기존 테이블을 조인해서 계산한다.
- 계산 로직은 5주차 `calc_metrics.py`의 구조를 따른다 — 정의서의 `계산.원천`·`집계`·`조인`·`조건`으로 SQL을 조립한다. 지표별 하드코딩 SQL을 만들지 않는다.

### 5-3. 이메일 = 초안 + 확정까지

- 8단계 "발송 확정"이 하는 일은 **최종 파일을 확정 저장하는 것**이다. 메일 서버에 연결하지 않는다.
- 초안에는 제목·수신자·본문·첨부 목록이 모두 들어간다. 8주차에 이 지점에 SMTP를 꽂으면 완성되는 형태여야 한다.
- 수신자는 설정 파일에 예시값으로 둔다. 실제 주소를 코드에 쓰지 않는다.

### 5-4. 독립 앱으로 만든다

- `customer-churn-dashboard`와 별도 폴더다. 그 앱의 코드를 import하지 않는다.
- 다만 **디자인 원칙과 공통 유틸은 승계한다** (7절 참고).
- 이유: 수강생이 이 앱을 따라 만든 뒤 본인 프로젝트로 이식해야 한다. 기존 앱에 얽혀 있으면 떼어내기 어렵다.

### 5-6. 데모 모드 — 자격증명 없이 흐름만 보여준다

BigQuery는 익명 접근을 허용하지 않는다. 그래서 자격증명을 넣을 수 없는 자리(공개 배포본을 남에게 보여줘야 하는 경우)에서는 실제 계산을 할 방법이 없다. 데모 모드는 그 자리에서 **계산만 흉내 내어 1~8단계 흐름 전체를 보여준다.** 구현은 `pipeline/demo.py`에 있다.

- **가짜는 숫자뿐이다.** BigQuery가 돌려주는 값만 만들어낸다. 스키마 대조·유효구간·최소표본·파생지표 정합성·합계 대조·리포트 조립은 **실제 코드가 그대로 돈다.** 판정 로직을 흉내 내면 이 모드는 아무것도 보여주지 못한다.
- **명시적으로 켠 경우에만 켜진다.** 환경변수 `AUTO_REPORT_DEMO=1` 또는 secrets의 `demo_mode = true` 둘뿐이다. **자격증명을 못 찾았다고 조용히 넘어오지 않는다** — 토큰이 만료된 것을 모른 채 가짜 숫자를 진짜로 읽는 것이 9절이 금지하는 거짓 보고다.
- **켜져 있으면 배너가 선다.** 본문 맨 위와 사이드바 양쪽에, 차단색(rose)으로, 접을 수 없게. 배너를 지우는 방법은 데모 모드를 끄는 것뿐이다.
- **진짜와 가짜를 한 화면에 섞지 않는다.** 기록된 실행을 불러온 화면(`loaded_run`)의 지표 값은 파일에서 읽은 진짜다. 이때 추이 차트는 지금 계산해야 해서 가짜가 나오므로 **그리지 않고 이유를 적는다.**
- 데모 모드의 프로젝트명은 `demo-no-bigquery`다. 실행 기록에 그대로 찍혀 나중에 구별된다.

실제로 계산한 결과는 `outputs/run_20260822_1424/` 한 벌을 저장소에 넣어 둔다. 배포본에서 **기존 실행 불러오기**로 열면 진짜 숫자를 볼 수 있다. 이 한 벌이 없으면 배포본에 실제 계산 결과가 하나도 남지 않는다.

### 5-5. 재현성

- 같은 파일을 같은 카탈로그로 넣으면 항상 같은 결과가 나와야 한다.
- 계산에 현재 시각을 쓰지 않는다. 기간은 업로드 파일의 데이터에서 판정하거나 사용자가 지정한 값을 쓴다.
- 실행마다 **실행 기록**(파일명·행수·카탈로그 갱신일·계산 시각·승인 시각)을 남긴다.

---

## 6. 검증 자동화 범위

3단계 검증은 **기계적으로 판정 가능한 것만** 수행한다. 판단이 필요한 검증은 하지 않고, **하지 않았다는 사실을 리포트에 명시한다.**

### 자동으로 하는 것

| 항목 | 판정 |
|---|---|
| 유효구간 밖 | 계산하지 않고 "데이터 없음"으로 표시. **0으로 처리하지 않는다** |
| 최소표본 미달 | 값은 내되 경고 표시. 결론 근거로 쓰지 않음을 명시 |
| 파생지표 정합성 | 기초지표를 재조합해 파생지표 값과 대조 |
| 전월 대비 이상 변동 | 임계값 초과 시 플래그. 전월 데이터가 없으면 "비교 불가"로 표시 |
| 합계 대조 | 세그먼트 합이 전체와 일치하는지 |

### 자동으로 하지 않는 것

| 항목 | 이유 |
|---|---|
| 혼입 변수 층화 | 어느 변수가 혼입인지는 업무 지식이 필요하다 |
| 역인과 검토 | 판단 |
| 가설 검정 | 사전 정의된 가설이 없으면 다중비교 문제를 통제할 수 없다 |

**리포트 한계 절에 다음 문장이 자동으로 들어간다:** 이 검증은 기계적 점검만 수행했으며, 혼입 변수·역인과 검토는 포함되지 않았다.

이 문장을 빼면 "검증 통과"가 실제보다 강한 신뢰를 주게 된다.

---

## 7. 디자인 원칙

**디자인 원칙과 기술 선택은 같은 폴더의 [DESIGN.md](./DESIGN.md)에 있다.** `customer-churn-dashboard`에서 이식한 문서이며, 화면·스타일·PDF를 건드리기 전에 그 문서를 먼저 읽는다. 원칙 14개와 라이브러리 함정 3건이 정리되어 있어, 같은 시행착오를 반복하지 않는다.

여기서는 **항상 지켜야 할 것**과 **이 앱에만 있는 규칙**만 적는다.

### 항상 지키는 것

| 원칙 | 내용 |
|---|---|
| 이상한 값이 보이면 즉시 고친다 | `undefined`·`nan`·빈 칸이 화면에 노출되면 원인을 찾아 고친다. 넘어가지 않는다 |
| 화면과 문서의 시각 언어 일치 | 화면에서 경고인 항목이 PDF·이메일에서 정상으로 보이면 안 된다 |
| 측정으로 확인한다 | 글자 크기·정렬을 눈대중으로 판단하지 않고 실제 렌더링을 확인한다 |
| 커밋은 요청받을 때만 | 코드를 고친 뒤 먼저 커밋·푸시하지 않는다 |

### 이 앱에만 있는 규칙 — 상태 색 대응

`status` 3종과 검증 결과를 화면·리포트·이메일에서 **같은 색으로** 표시한다. 이 대응을 바꾸지 않는다.

| 상태 | 색 | Tailwind |
|---|---|---|
| 통과 / OK | emerald | `#10b981` |
| 경고 / 표본부족 | amber | `#f59e0b` |
| 차단 / 검증 실패 | rose | `#f43f5e` |
| 데이터 없음 / 유효구간 밖 | slate | `#64748b` |

**데이터 없음을 회색으로 두는 것이 중요하다.** 초록이나 빨강으로 칠하면 "정상" 또는 "문제"로 읽히지만, 실제로는 판단할 근거가 없는 상태다.

### 하드 제약

- PDF는 `fpdf2` + Noto Sans KR 서브셋으로 만든다. Malgun Gothic은 재배포 불가라 쓸 수 없다.
- `kaleido`(Plotly 정적 이미지)는 이 환경에서 불안정하다. **PDF에 차트 이미지를 넣지 않는다** — 텍스트·표 중심으로 구성한다.

---

## 8. 폴더 구조

```
auto-report/
  CLAUDE.md              이 문서. 코드를 쓰기 전에 읽는다
  DESIGN.md              디자인 원칙·라이브러리 함정. 화면을 건드리기 전에 읽는다
  app.py                 Streamlit 진입점. 8단계 흐름과 승인 게이트를 담당
  config.py              프로젝트ID·데이터셋·스테이징 테이블명·수신자 예시값
  common.py              색상 상수·폰트·공통 유틸 (DESIGN 원칙 반영)

  catalog/
    export_catalog.py    위키 → JSON 변환 (06_metrics + 02_data)
    metrics_catalog.json 지표 명세 스냅샷 (앱이 읽는 유일한 정의 원천)
    schema_catalog.json  테이블 스키마 스냅샷 (업로드 파일 판정 기준)

  pipeline/
    demo.py              데모 모드 — 가짜 BigQuery 클라이언트와 on/off 스위치 (5-6절)
    intake.py            1단계 — 파일 수신·기본 검사
    profile.py           2단계 — 스키마 판정·결측·행수 점검
    calculate.py         2단계 — 스테이징 적재 후 지표 계산
    validate.py          3단계 — 기계적 검증 게이트
    report.py            6단계 — 리포트 생성 (마크다운 + PDF)
    email_draft.py       7단계 — 이메일 초안 생성
    send.py              8주차 자리. 실제 발송 코드는 6주차 범위 밖 — 지금은
                         NotImplementedError만 있고 app.py는 이 파일을 부르지 않는다

  outputs/
    run_YYYYMMDD_HHMM/   실행 단위 폴더 (계산결과·리포트·이메일초안·실행기록)
    run_20260822_1424/   예외로 저장소에 넣은 한 벌. 실제 BigQuery로 돌린 결과이며
                         배포본에서 진짜 숫자를 보여주는 근거다 (5-6절)

  fonts/                 Noto Sans KR 서브셋 (PDF용)
  requirements.txt
```

- 8단계(발송 확정)의 확정 로직은 `app.py`의 승인 게이트에 있다. `outputs/run_*/`에 확정 표시를 남기는 것으로 끝나며, 실제 발송은 `pipeline/send.py`에 자리만 있고 아직 연결되지 않는다.
- 실행마다 새 폴더를 만든다. 이전 실행 결과를 덮어쓰지 않는다.

---

## 9. 절대 하지 않는 것

| 금지 | 왜 |
|---|---|
| 지표 정의를 앱 코드에 쓰기 | 정의가 두 곳에 생기면 반드시 어긋난다 |
| 노트명↔테이블명 매핑을 코드에 하드코딩 | 위키에 `bq_table`을 쓰면 되는 일이다 |
| 컬럼 표의 편집 표시를 파서가 해석 | 노트를 정리할 문제를 코드로 떠안게 된다 |
| 카탈로그에 없는 테이블을 추측해 통과 | 잘못된 스키마로 계산이 돌아간다 |
| 유효구간 밖·표본부족을 0이나 빈 값으로 조용히 처리 | 거짓 보고가 된다. "데이터 없음"과 "값이 0"은 다르다 |
| 혼입·역인과를 안 봤는데 "검증 완료"로 표시 | 실제보다 강한 신뢰를 준다 |
| 사용자 승인 없이 다음 단계 실행 | 게이트가 없으면 자동화가 아니라 방치다 |
| 단계 건너뛰기 허용 | 3단계 검증을 안 거친 숫자가 리포트에 들어간다 |
| DML로 원본 테이블 수정 | 샌드박스에서 막히고, 원본이 오염된다 |
| 실제 이메일 발송 | 6주차 범위 밖. 8주차에 구현한다 |
| 계산에 현재 시각 사용 | 재현성이 깨진다 |
| 데모 모드를 배너 없이 돌리기 | 가짜 숫자가 진짜로 읽힌다. 배너가 이 모드의 유일한 안전장치다 |
| 자격증명이 없다고 데모 모드로 자동 전환 | 인증이 끊긴 것을 모른 채 가짜를 보게 된다. 명시적으로만 켠다 |
| 한 화면에 진짜 숫자와 가짜 숫자 섞기 | 어느 쪽이 진짜인지 구분할 방법이 없어진다 |
| 지표별 하드코딩 SQL | 5주차에 이미 한 번 실패한 방식이다 |

---

## 10. 용어

| 용어 | 뜻 |
|---|---|
| **카탈로그** | 위키에서 추출한 JSON 스냅샷 2종(지표 명세·테이블 스키마). 앱이 읽는 유일한 정의 원천 |
| **스테이징 테이블** | 업로드 파일을 담는 임시 BigQuery 테이블. 매 실행마다 교체된다 |
| **게이트** | 사용자 승인 없이 통과할 수 없는 지점. 2·5·8단계에 있다 |
| **status** | 지표 계산 결과의 상태. `OK` / `유효구간 밖` / `표본부족` |
| **실행 단위** | 파일 하나를 넣어 확정까지 가는 한 사이클. `outputs/run_*/`에 대응 |

---

## 11. 갱신 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-11 | 최초 작성. 8단계 흐름·위키 연동 구조·설계 결정 5건 확정 |
| 2026-08-11 | `customer-churn-dashboard/DESIGN.md`를 이 폴더로 이식. 7절을 "항상 지키는 것 + 상태 색 대응 + 하드 제약"으로 재구성하고, 나머지 원칙은 DESIGN.md에 위임 |
| 2026-08-11 | 카탈로그를 2종으로 분리. Day1 설계 중 **지표 명세만으로는 업로드 파일 스키마를 판정할 수 없다**는 것이 드러나, `02_data`의 컬럼 정의를 `schema_catalog.json`으로 함께 export하도록 3·8·10절 수정 |
| 2026-08-18 | 실제 위키로 export를 돌려보며 발견한 두 문제를 3절에 규칙으로 추가. ① 노트명(`data_usage_history`)과 테이블명(`usage_history`) 불일치 — 키는 테이블명으로 하고 추정 여부를 표시 ② 컬럼 표에 섞인 편집 표시(취소선·화살표) — 파서가 해석하지 않고 경고 후 위키를 고친다. 9절 금지 항목 3건 추가 |
| 2026-09-18 | Streamlit Community Cloud 배포 대응. ① `make_client()`가 **데모 → secrets(서비스 계정) → `BQ_PROJECT` → ADC** 순으로 자격증명을 찾는다 — 배포 환경에는 ADC가 없다 ② **데모 모드**(5-6절) 신설. BigQuery는 익명 접근이 없으므로 자격증명을 넣을 수 없는 자리에서는 계산을 흉내 내어 흐름만 보여준다. 가짜 클라이언트 구현을 `dev_tools/fast_test_server.py`에서 `pipeline/demo.py`로 옮기고 dev_tools는 그것을 import한다 — 구현이 두 벌이면 한쪽만 고쳐져 서로 다른 숫자를 낸다 ③ 실제 실행 한 벌(`outputs/run_20260822_1424/`)을 저장소에 포함 ④ 9절 금지 3건 추가, 배포 절차는 `DEPLOY.md` |
| 2026-08-22 | 6주차 범위 완료. 1~8단계 전부 화면(app.py)과 CLI(run_pipeline.py) 양쪽에서 구현됨 — 판정·계산·검증·대시보드·리포트·이메일 초안·발송 확정 게이트, 실행 기록(`pipeline/runlog.py`), 재현성 테스트(`tests/test_reproducible.py`) 포함. 8주차에 남은 것은 `pipeline/send.py`의 실제 SMTP 발송 하나뿐이며, 지금은 자리만 있고 `app.py`는 이 파일을 부르지 않는다 |
